-        <button type="button" title="Light appearance" className="flex h-11 w-11 items-center justify-center rounded-xl border border-[#D7E5F6] bg-white/70 text-[#0B1851] shadow-sm"><Sun size={22}/></button>
-        <button type="button" title="English interface" className="flex h-11 min-w-[100px] items-center justify-center gap-5 rounded-xl border border-[#D7E5F6] bg-white/70 px-4 text-[15px] font-bold shadow-sm">EN <ChevronDown size={17}/></button>
-      </div>
-    </header>
 
-    <main className="relative z-10 mx-auto grid w-full max-w-[1110px] grid-cols-[430px_1fr] items-center gap-[78px] px-6 pb-[180px] pt-[32px]">
-      <section className="rounded-[14px] border border-white/80 bg-white/92 p-[28px] shadow-[0_26px_70px_rgba(48,111,177,.10)] backdrop-blur-xl">
-        <h1 className="text-[39px] font-extrabold leading-none tracking-[-.04em]">Welcome back</h1>
-        <p className="mt-2 text-[18px] font-medium text-[#586FB5]">Sign in to your MCP Control Plane</p>
+      <header className="relative z-10 mx-auto flex h-[74px] max-w-[1220px] items-center justify-between px-6">
+        <ProductMark compact />
+        <div className="flex items-center gap-2 rounded-full border border-slate-200 bg-white/85 px-3 py-1.5 text-[10px] font-bold text-slate-500 shadow-sm backdrop-blur">
+          <span className="h-2 w-2 rounded-full bg-emerald-400 shadow-[0_0_0_4px_rgba(52,211,153,.12)]" />
+          Production control plane
+        </div>
+      </header>
 
-        {error ? <div className="mt-5 flex items-start justify-between gap-3 rounded-xl border border-rose-200 bg-rose-50 px-3 py-2.5 text-[12px] text-rose-700"><span>{error}</span><button type="button" onClick={() => setError('')}><X size={15}/></button></div> : null}
-        {bootstrapRequired ? <div className="mt-5 rounded-xl border border-cyan-200 bg-cyan-50/70 px-3 py-2.5 text-[11px] text-cyan-900"><b>First-time setup required.</b> No browser administrator exists yet. <button type="button" className="font-bold underline" onClick={() => setBootstrapOpen(true)}>Initialize admin</button></div> : null}
+      <main className="relative z-10 mx-auto grid min-h-[calc(100vh-74px)] max-w-[1180px] grid-cols-[1.08fr_.92fr] items-center gap-12 px-6 pb-20">
+        <section className="hidden pr-4 lg:block">
+          <div className="mb-6 inline-flex items-center gap-2 rounded-full border border-blue-100 bg-blue-50/80 px-3 py-1.5 text-[10px] font-black uppercase tracking-[.16em] text-blue-700">
+            <ShieldCheck size={14} /> Secure administrator access
+          </div>
+          <h1 className="max-w-[610px] text-[54px] font-black leading-[.98] tracking-[-.055em] text-[#0b1835]">
+            One control plane.<br/><span className="bg-[linear-gradient(90deg,#1769e0,#0fa8cf_52%,#16a477)] bg-clip-text text-transparent">Real routing data.</span>
+          </h1>
+          <p className="mt-5 max-w-[560px] text-[15px] font-medium leading-7 text-slate-500">
+            Sign in to the Cloudflare-hosted OmniRoute control plane. Browser authentication stays separate from gateway API keys and routing authority remains with OmniRoute.
+          </p>
 
-        <form onSubmit={handleSubmit} className="mt-7 space-y-5">
-          <label className="block"><span className="mb-2 block text-[15px] font-bold">Email</span><span className="flex h-[49px] items-center gap-3 rounded-[9px] border border-[#C9DDF5] bg-white px-4 shadow-[inset_0_1px_3px_rgba(20,60,100,.04)] focus-within:border-[#4BAAF6]"><Mail size={19} className="text-[#5872B2]"/><input autoComplete="username" type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="admin@example.com" required className="min-w-0 flex-1 bg-transparent text-[14px] text-[#0B1851] outline-none placeholder:text-[#8EA0C2]"/></span></label>
-          <label className="block"><span className="mb-2 flex items-center justify-between"><b className="text-[15px]">Password</b><button type="button" className="text-[13px] font-semibold text-[#1D75E6]" onClick={() => setError('Password recovery is administrator-controlled. Contact your administrator or use the configured bootstrap/recovery procedure.')}>Forgot password?</button></span><span className="flex h-[49px] items-center gap-3 rounded-[9px] border border-[#C9DDF5] bg-white px-4 focus-within:border-[#4BAAF6]"><Lock size={19} className="text-[#5872B2]"/><input autoComplete="current-password" type={showPassword ? 'text' : 'password'} value={password} onChange={(e) => setPassword(e.target.value)} required className="min-w-0 flex-1 bg-transparent text-[14px] outline-none"/><button type="button" onClick={() => setShowPassword((v) => !v)} className="text-[#5872B2]">{showPassword ? <EyeOff size={20}/> : <Eye size={20}/>}</button></span></label>
-          <button disabled={isSubmitting} type="submit" className="flex h-[52px] w-full items-center justify-center gap-3 rounded-[9px] bg-gradient-to-r from-[#4AA8F8] via-[#2DC6D5] to-[#37D8A6] text-[16px] font-bold text-white shadow-[0_9px_22px_rgba(45,183,211,.23)] transition hover:brightness-[1.02] disabled:cursor-not-allowed disabled:opacity-50">{isSubmitting ? 'Signing in…' : 'Sign in'} <ArrowRight size={20}/></button>
-        </form>
+          <div className="mt-8 grid max-w-[600px] grid-cols-3 gap-3">
+            {[
+              [ShieldCheck, 'Server sessions', 'Revocable browser auth'],
+              [Network, 'OmniRoute authority', 'No second edge router'],
+              [Activity, 'Live control APIs', 'Real observed state'],
+            ].map(([Icon, title, detail]) => {
+              const C = Icon as React.ComponentType<{ size?: number; className?: string }>;
+              return <div key={String(title)} className="rounded-2xl border border-white/80 bg-white/75 p-4 shadow-[0_14px_40px_rgba(30,64,175,.07)] backdrop-blur-xl">
+                <div className="mb-3 grid h-9 w-9 place-items-center rounded-xl bg-slate-950 text-white"><C size={17}/></div>
+                <div className="text-[11px] font-black text-slate-800">{String(title)}</div>
+                <div className="mt-1 text-[9px] font-medium leading-4 text-slate-400">{String(detail)}</div>
+              </div>;
+            })}
+          </div>
+        </section>
+
+        <section className="mx-auto w-full max-w-[455px] rounded-[26px] border border-white bg-white/95 p-7 shadow-[0_28px_80px_rgba(29,78,140,.14)] backdrop-blur-xl">
+          <ProductMark />
+          <div className="mt-7">
+            <h2 className="text-[29px] font-black tracking-[-.04em] text-slate-950">Administrator sign in</h2>
+            <p className="mt-1.5 text-[12px] font-medium text-slate-500">Use your admin username or email and password.</p>
+          </div>
+
+          {error ? <div className="mt-5 flex items-start justify-between gap-3 rounded-xl border border-rose-200 bg-rose-50 px-3 py-2.5 text-[11px] font-semibold text-rose-700"><span>{error}</span><button type="button" onClick={() => setError('')}><X size={14}/></button></div> : null}
+          {bootstrapRequired ? <div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 px-3 py-3 text-[10.5px] leading-5 text-amber-900"><b>First administrator setup is required.</b> Create the real browser admin before signing in. <button type="button" className="ml-1 font-black text-amber-950 underline underline-offset-2" onClick={() => setBootstrapOpen(true)}>Initialize admin</button></div> : null}
 
-        <div className="my-6 flex items-center gap-3 text-[12px] text-[#6579B3] before:h-px before:flex-1 before:bg-[#DDE7F3] after:h-px after:flex-1 after:bg-[#DDE7F3]">or continue with</div>
-        <div className="grid grid-cols-3 gap-3">{oauthButtons.map((provider) => <button key={provider} type="button" disabled={!providers[provider]} title={providers[provider] ? `Continue with ${providerLabels[provider]}` : `${providerLabels[provider]} OAuth is not configured`} onClick={() => startOAuth(provider)} className="flex h-[47px] items-center justify-center gap-2 rounded-[9px] border border-[#D5E2F2] bg-white text-[14px] font-bold text-[#0B1851] shadow-sm transition hover:bg-[#F8FBFF] disabled:cursor-not-allowed disabled:opacity-45"><ProviderLogo provider={provider}/>{providerLabels[provider]}</button>)}</div>
-        <p className="mt-7 text-center text-[12px] text-[#6376AD]">Don&apos;t have an account? <span className="font-semibold text-[#1269D9]">Contact your administrator</span></p>
-      </section>
+          <form onSubmit={handleSubmit} className="mt-6 space-y-4">
+            <label className="block">
+              <span className="mb-2 block text-[11px] font-black uppercase tracking-[.09em] text-slate-600">Username or email</span>
+              <span className="flex h-[50px] items-center gap-3 rounded-xl border border-slate-200 bg-slate-50/70 px-4 transition focus-within:border-blue-400 focus-within:bg-white focus-within:ring-4 focus-within:ring-blue-50">
+                <UserRound size={18} className="text-slate-400"/>
+                <input autoComplete="username" value={identifier} onChange={(event) => setIdentifier(event.target.value)} placeholder="admin or admin@example.com" required className="min-w-0 flex-1 bg-transparent text-[13px] font-semibold text-slate-900 outline-none placeholder:font-medium placeholder:text-slate-400"/>
+              </span>
+            </label>
 
-      <section className="pb-16">
-        <div className="text-[13px] font-bold tracking-[.36em] text-[#7690C8]">OPEN TOOLS<br/>A BRIGHTER TOMORROW</div>
-        <h2 className="mt-10 text-[56px] font-extrabold leading-[.98] tracking-[-.045em]">More capable<br/><span className="bg-gradient-to-r from-[#2184E7] via-[#18AACE] to-[#27C98D] bg-clip-text text-transparent">tomorrow.</span></h2>
-        <div className="mt-9 h-[3px] w-10 bg-[#16B7B8]"/>
-        <p className="mt-8 text-[18px] leading-[1.6] text-[#6276AF]">Unified control for models,<br/>providers, and AI tools.<br/>Simple. Open. Powerful.</p>
-        <blockquote className="mt-10 text-[17px] italic leading-8 text-[#7384B6]">“ The best tools amplify<br/><span className="pl-5">human potential. ”</span></blockquote>
-      </section>
-    </main>
+            <label className="block">
+              <span className="mb-2 flex items-center justify-between"><b className="text-[11px] uppercase tracking-[.09em] text-slate-600">Password</b><button type="button" className="text-[10px] font-bold text-blue-600" onClick={() => setError('Password recovery is administrator-controlled. Use the configured recovery procedure if needed.')}>Recovery</button></span>
+              <span className="flex h-[50px] items-center gap-3 rounded-xl border border-slate-200 bg-slate-50/70 px-4 transition focus-within:border-blue-400 focus-within:bg-white focus-within:ring-4 focus-within:ring-blue-50">
+                <Lock size={18} className="text-slate-400"/>
+                <input autoComplete="current-password" type={showPassword ? 'text' : 'password'} value={password} onChange={(event) => setPassword(event.target.value)} required className="min-w-0 flex-1 bg-transparent text-[13px] font-semibold text-slate-900 outline-none"/>
+                <button type="button" onClick={() => setShowPassword((value) => !value)} className="text-slate-400">{showPassword ? <EyeOff size={18}/> : <Eye size={18}/>}</button>
+              </span>
+            </label>
 
-    <footer className="absolute inset-x-[68px] bottom-7 z-10 flex items-center justify-between text-[12px] text-[#6979AC]"><div><b className="text-[#1269D9]">DreamWorker</b><span className="mx-4">·</span>The future is created today.</div><div className="flex gap-9"><span>v1.8.0</span><span>Documentation ↗</span><span>Support ↗</span></div></footer>
