{
  "name": "cf-control-mcp",
  "version": "1.8.0",
  "private": true,
  "description": "Private remote MCP server on Cloudflare Workers for owner-approved Cloudflare account control.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test \"build-test/test/*.test.js\"",
    "test:internet": "npm run build:test && node --test \"build-test/test/*.test.js\"",
    "secrets": "echo 'Run: wrangler secret put MCP_AUTH_TOKEN && wrangler secret put CLOUDFLARE_API_TOKEN'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
